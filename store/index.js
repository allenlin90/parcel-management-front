export const state = () => ({
    host: 'https://parcel-api.makesend.ninja'
});

export const actions = {}
export const mutations = {}
export const getters = {
    host(state) {
        return state.host;
    }
}